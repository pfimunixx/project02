#-------------------------------------------------------------------------------
# parameter
#-------------------------------------------------------------------------------
parameters:
  id:
    in: path
    name: id
    schema:
      type: integer
      format: int64
    required: true

#-------------------------------------------------------------------------------
# requestBodies
#-------------------------------------------------------------------------------
requestBodies:
  UserDto:
    description: User
    required: true
    content:
      application/json:
        schema:
          $ref: schema.yml#/schema/User

#-------------------------------------------------------------------------------
# responses
#-------------------------------------------------------------------------------
responses:
  UserDto:
    description: User
    required: true
    content:
      application/json:
        schema:
          $ref: schema.yml#/schema/User
  UserListDto:
    description: UserList
    required: true
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: schema.yml#/schema/User